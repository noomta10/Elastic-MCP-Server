# Project Nebula: Frontend Portal
This is the main user interface for the Nebula ecosystem.

## Setup
1. Run `npm install`
2. Run `npm start` to launch on port 3000.

## Features
- Real-time dashboard for space telemetry.
- Dark mode support.
- User authentication via OAuth.